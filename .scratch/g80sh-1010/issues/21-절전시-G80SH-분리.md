# 21 — 화면이 꺼진 동안 G80SH 를 "사용 안 함"으로 두면 폭풍이 멎는가

Type: task
Status: in progress (A안 기각, B안 대기)

사용자 요청(09-24 09:5x): 완화책(ffmpeg 로 D3 차단)은 적용하지 않는다 — "방식이 별로". 대신 절전 시 G80SH 를
"사용 안 함"으로 바꾸는 방식 검토. 시험은 3분 구간 + 입력 시 복귀 확인.

## A안 — 화면 구성에서 분리 (G50F 단독, CCD SetDisplayConfig) — 기각

`tools\g80sh-exp-detach.ps1` (감시자 일시 정지, VIDEOIDLE 60 임시, 끝나고 원복)

```
09:56:38~09:59:38  평소 구성(G80SH+G50F)  사이클 4
09:59:43~10:02:43  G80SH 분리(G50F 만)    사이클 4     ← 차이 없음
10:02:43  입력 주입 → 1.0초 뒤 G80SH 붙일 수 있음 → 평소 구성 복귀 ok (입력부터 7.6초)
```

화면 구성에서 빼도 장치 연결은 남아 GPU 복귀 재감지·해제가 그대로다.
→ 원격 접속 중(06:46~09:01) 폭풍 0건은 **원격 화면 생성으로 GPU 가 계속 일해 D3 에 못 들어간 것**으로 정리(20-8 과 같은 원리).
부수: 입력 복귀 경로는 정상(1초 안에 붙일 수 있고 7.6초에 원래 구성).

## B안 — 장치 관리자 "사용 안 함"(모니터 devnode Disable) — 미시험, 사용자 결정 대기
기대 낮음: 해제는 GPU 연결 감지(ConnectorChange)에서 시작하므로 모니터 devnode 를 꺼도 재감지는 남을 가능성이 크다.
