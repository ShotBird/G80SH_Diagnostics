# 22 — 별도 프로그램 없이 설정만으로 D3 를 막을 수 있는가 — 없다

Type: task
Status: resolved (기각)

사용자 요구(09-24): ffmpeg 완화책은 "방식이 별로" — 설정만으로 D3 를 막는 방법. 폭풍이 나면 즉시 파기.

| 설정 | 결과 |
|---|---|
| Windows 전원 옵션 PCI Express 링크 상태 전원 관리 | **이미 '해제'**(고성능 구성표 AC·DC) — 이 상태에서 폭풍. 효과 없음 확정 |
| AMD `EnableUlps`·`EnableUlps_NA` = 0 (+GPU 재시작) | 11:01:10 관측 시작 → **GPU D3 2회**, 11:01:45 폭풍 → 0.7분 만에 파기. 원복 확인 |
| (20) `PP_ULPSDelayIntervalInMilliSeconds` 90초 | 경계 불변 |
| (20) `PP_GPUPowerDownEnabled` = 0 | D3 그대로 |

→ 이 D3 는 AMD 의 ULPS/BACO 스위치가 아니라 **Windows 그래픽 커널 런타임 전원 관리(PoFx)** 가 건다(19 의 스택과 일치).
사용자가 끌 수 있는 공식 설정은 찾지 못했다. 설정으로 D3 를 막는 길은 없다.

참고 자료(EnableUlps 위험): AMD 포럼 1건(날짜 미상, 9070 XT 영상 디코더 먹통·HAGS 끄면 해결), 반대 사례 블로그(2026-07-25, 끄자 유휴 충돌 해결),
Guru3D(2025-06-11, "ULPS 끄기 = 장치 전원 차단 기능 끄기"). 빈도는 드물다.

부수 — Windows 화면 꺼짐 타이머 이상(09-24 두 번):
- 09:01 원격 종료·감시자 화면 깨우기 뒤 22분간 안 꺼짐 → VIDEOIDLE 900→60 재적용 즉시 꺼짐(09:24)
- 10:38 시험 입력 뒤 17분간 안 꺼짐, 60초로 줄여도 안 꺼짐 → PC 잠금(LockWorkStation) 1분 뒤 꺼짐(10:58)
  두 번 모두 화면 켜기 요청(powercfg /requests DISPLAY) 없음. SendInput 가짜 입력 뒤라는 공통점 — 원인 미확정.
