# 23 — Windows 그래픽 커널 설정으로 D3 를 막을 수 있는가 — 기각 4/4, 시험 직후 BSOD

Type: task
Status: resolved (기각) + 사고 기록

사용자 지시(09-24): ffmpeg 제외, 기준 측정 없이 개선안만 시험, 폭풍 나면 즉시 파기.
근거: dxgkrnl.sys 문자열에서 실제로 읽는 이름 확인(공식 문서 없음). AMD amdkmdag.sys 에서 RTPM(런타임 전원 관리) 계열 이름 확인.
도구: `tools\g80sh-exp-regseq.ps1` (후보마다 값 → GPU 재시작 → 30초 여유 → 3분 관측, 폭풍 1건이면 파기 → 원복 → GPU 재시작)

| 후보 | 결과 |
|---|---|
| `GraphicsDrivers\Power\DisableD3Requests` = 1 | 파기 — 1.1분, GPU D3 2회 |
| `GraphicsDrivers\Power\EnableRuntimePowerManagement` = 0 | 파기 — 0.7분, GPU D3 1회 |
| `GraphicsDrivers\Power\DefaultD3TransitionLatencyIdleMonitorOff` = 1 | 파기 — 0.1분(관측 창 D3 0회 — 여유 30초 안의 D3 로 보임, **판정 불확실**) |
| AMD `KMD_RTPMEnabled` = 0 | 파기 — 0.6분, GPU D3 2회 |

한계: `GraphicsDrivers\Power` 가 dxgkrnl 이 실제로 읽는 위치인지 확인 못 했다(위치가 틀리면 값이 무시된다). 이름은 실재.

## 사고 — 11:18 BSOD 0x133 (DPC_WATCHDOG_VIOLATION)

- 11:08~11:17 사이 GPU 드라이버 재시작 **8회**(후보당 2회). 마지막 재시작(11:17:41) 뒤 G50F(SAM79DF)가 재인식되지 않았고
  (감시자 HOME 실패 11:17:43~11:18:30), 입력 주입 뒤 PowerShell 의 장치 조회가 응답 없음 → 11:18:12 비정상 종료 → 11:24 재부팅.
- 덤프: `C:\Windows\Minidump\092426-20000-01.dmp` (분석 도구 미설치 — 모듈 미특정). 짧은 간격의 반복 GPU 재시작이 원인일 가능성이 크다.
- 재부팅 뒤 확인(11:25): 모니터 3대 정상, 레지스트리 전부 원래 값(Power 키 없음, EnableUlps 1/1, PowerDown 1, ULPSDelay 30000, KMD_RTPM 없음),
  VIDEOIDLE 900, 임시 작업·ffmpeg 없음.
- **교훈: GPU 드라이버 재시작을 짧은 간격으로 반복하는 시험은 하지 않는다.** 필요하면 재시작 사이 충분한 간격 + 한 세션 1~2회로 제한.

### 덤프 분석 (09-24 11:4x, WinDbg cdb `!analyze -v`)

```
BUGCHECK 0x133 DPC_WATCHDOG_VIOLATION, Arg1=1 (누적 시간 초과), 감시 주기 0x1e00 틱
DPC_TIMEOUT_TYPE  DPC_QUEUE_EXECUTION_TIMEOUT_EXCEEDED
MODULE / IMAGE    amdkmdag.sys (AMD 그래픽 드라이버)
FAILURE_BUCKET    0x133_ISR_amdkmdag!unknown_function
스택              amdkmdag+0x1710964 → nt!KeStallExecutionProcessor → (클럭 인터럽트) → KeAccumulateTicks → KeBugCheckEx
```

AMD 드라이버가 높은 IRQL 에서 `KeStallExecutionProcessor` 로 하드웨어 응답을 바쁘게 기다리다 감시 한도를 넘었다.
직전 상황(8분간 GPU 재시작 8회, 마지막 재시작 뒤 G50F 미인식)과 맞으면 **반복 재시작 중 모니터 응답을 기다리다 터진 것** — 시험이 유발한 사고.
덤프 사본 `_evidence\bsod-0924\` (git 제외). 디버거는 winget `Microsoft.WinDbg` (cdb 사본 `%LOCALAPPDATA%\dbg-amd64`).

### 용량 정리 (09-24, 사용자 지시 "남길 거 빼고 정리")

- 지움: `_evidence` 의 시험 원본 ETL(deeptest·exp-trcc/logi/all/ulps90/pdown0/detach/regseq/disable/ulps0·conntest) 13.1 GB,
  `C:\symbols` 3.8 GB(재다운로드 가능), 세션 임시폴더 1.4 GB → 약 18 GB.
- 남김: 제출 패키지, 상시 캡처(g80sh-dxg-*), `exp-busy`(A-B-A 30분 0건) 와 `stacktest` 원본, 모든 로그·CSV·요약 텍스트.
- 상시 캡처는 하루 50~130 MB 증가(git 압축 후 .git 49 MB) — 보관 정책은 미정.
