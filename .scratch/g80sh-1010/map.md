# G80SH 1010 반복 — 원인 확정과 근원개선

## Destination

`DISPLAY\SAM7B0C\7&16b8ddb4&0&UID256`이 약 45초마다 제거(Kernel-PnP 1010)되는 현상에 대해,
**(a) 재열거를 촉발하는 주체를 ETW 증거로 확정**하고, 그 위에서
**(c) EDID 프로파일 이중화 자체를 없애는 근원개선 경로를 확정**한다.

**(a) 도달 완료** — [02](issues/02-선후-판정.md)가 ETW 원자료로 확정했다.
**(c) 도달 완료, 다만 답이 "PC 쪽 경로 없음"이다** — 후보가 ①(모니터 펌웨어/하드웨어)
하나로 좁혀졌고, ①은 PC 에서 실행할 수 없다.

> **⚠ 도착지가 소진됐다. 다시 그어야 한다.** 아래 「도착지 재설정」 참조.

## 도착지 재설정 — 결정 대기

(c)를 "PC 에서 EDID 이중화를 없앤다"로 정의했는데, 그 경로가 전부 막혔다.
남은 선택은 셋이고, 어느 쪽이든 **이 맵이 아니라 새 맵이다**(도착지를 다시 그으면 새 효정).

1. **(c)를 여기서 닫는다** — "PC 쪽 근원개선 경로 없음"이 결론. 맵 종료.
2. **도착지를 "삼성이 고치게 만드는 것"으로 옮긴다** — 범위 밖이던 (b) 제출 트랙이
   맵 안으로 들어온다. 자료는 `_evidence\삼성제출_대장.md`에 이미 쌓여 있다.
3. **관측을 계속한다** — 진행 중인 [10](issues/10-오디오-엔드포인트.md)과
   대기 중인 [11](issues/11-포트교체-효과.md)만 남기고, 결과가 나오면 다시 판단한다.

~~현재는 3으로 굴러가고 있다(실험이 진행 중이므로).~~ 1·2는 사용자 결정 사항.

> **2026-09-20 갱신.** 10 이 기각으로 닫혔다. 3을 지탱하던 "진행 중인 실험"이 없어졌고,
> 남은 것은 [11 — GPU DP 포트 교체](issues/11-포트교체-효과.md) 하나다. 11 은 사용자 손이
> 필요하고(HITL), 사용자가 걸어둔 방아쇠(재발하면 그때)가 **09-20 14:33 에 당겨졌다.**
> 11 을 태우고 나면 3에 남는 것이 없다 — **그 시점에 1이냐 2냐를 정해야 한다.**

## Notes

**도메인**: Windows 디스플레이 열거 — Kernel-PnP, CCD API, DxgKrnl ETW, EDID, DP 1.4 DSC,
AMD Adrenalin. 대상은 Odyssey OLED G8 G80SH (LS32HG802SKXKR, S/N HNBL700092,
펌웨어 M-H800GGAA-1002.2 최신), 대조군은 G50F(`SAM79DF`).
GPU는 **AMD Radeon RX 9070 XT**, 드라이버 `32.0.31041.1004` (2026-08-17 설치).

**기존 문서**
- `RemoteDisplaySwitch\docs\G80SH_장치인식_문제분석보고서.md` — 4-3항·5항·8항이 09-19에 뒤집혔다
- `_evidence\2026-09-18_추가분석.md` — 7항(미해결)은 02가 해결
- `_evidence\삼성제출_대장.md` — 제출에 영향을 주는 결과의 누적 대장
- `_evidence\현재실험.md` — **지금 무엇을 재고 있는지. 세션 시작 시 먼저 읽을 것**

**상시 규칙**

1. **우회책을 제안하지 않는다.** 근원개선은 EDID 이중화 자체를 없애는 것만 뜻한다.
   단 **측정을 위해 화면을 끄는 것은 관측 행위**이므로 허용된다.
   (09-19 GPU 오디오 비활성화는 사용자 요청으로 진행한 우회책 — [10](issues/10-오디오-엔드포인트.md)에 성격 명시)
2. **만들었다고 기록하기 전에 실재를 확인한다.** `Test-Path` / `schtasks /query` /
   프로세스 목록 / `logman query -ets`. 09-18의 오기록이 잘못된 가설로 이어진 전례가 있다([05](issues/05-관측자-효과.md)).
3. **단일 구간 관측으로 판정하지 않는다.** 30분 절전에 0건이 나올 확률이 **원래 45%**다
   ([09](issues/09-야간-변수-전수시험.md)). **며칠 단위 발생률**로만 판정한다 —
   `tools\g80sh-uptime-windows.ps1`. 09-19에만 이 함정을 다섯 번 밟았다.
4. **변수는 하나씩.** 조치 둘을 겹치면 귀속이 불가능해진다(09-19 21:11 삼성 앱 초기화와
   21:15 오디오 비활성화가 4분 차이로 겹쳐 [10](issues/10-오디오-엔드포인트.md) 귀속 불가).
5. 티켓을 풀 때 `grilling`과 `domain-modeling` 스킬을 호출한다.
6. 관리자 권한은 `RemoteDisplaySwitch\admin_hook.ps1`을 두고
   `Remote Display Restore (logon)` 작업을 실행해서 얻는다(실행 후 자동 삭제).
   훅 스크립트는 **단순하게** 쓸 것 — 복잡하면 중간에 죽어 출력이 안 나온다(09-19에 네 번).
7. **제출 대장을 갱신한다.** 제출 진술을 바꾸는 결과는 `_evidence\삼성제출_대장.md`에 쌓는다.

**버전 관리**: `C:\dev\PC` = git repo, 원격 `G80SH_Diagnostics` (**private**).
하위 프로젝트 2개와 `_archive`, 활성 `g80sh-live.etl`은 제외.

## Decisions so far

- **도착지 확정**: (a) 원인 확정 + (c) 근원개선. (b) 삼성 제출은 범위 밖, 기록만.
- [01 — ETW 캡처 확보](issues/01-etw-캡처-확보.md): 09-18에 "구성했다"던 측정 장치가 실재하지
  않아 처음부터 다시 만들었다. **rev.2는 순환 버퍼(512MB)를 상시 돌리고 트리거 때 덤프**한다 —
  트리거 후에 세션을 켜면 방아쇠가 끄는 순간에 있을 때 매번 놓친다. 09-19에 폭풍 4회 포착.
- [02 — 48초 타이머의 주인](issues/02-선후-판정.md) **★ 핵심 결정**:
  **OS/드라이버가 먼저 조회를 건다.** 폭풍 사이클은 `Kernel-PnP 220`(GPU 자식 재조회)이
  앞서고 `DxgKrnl` StatusChangeNotify는 그 뒤에 온다. **220보다 앞선 10011은 0건**.
  정상 전환 기준선은 정반대(10011이 20여 건 선행). 재설치 후에도 -42ms로 동일 재현.
  → 모니터가 링크를 끊어 알린 것이 아니다. AMD miniport가 스스로
  `IoInvalidateDeviceRelations`를 걸었다.
- [03 — 발생 조건 재확정](issues/03-발생조건-재확정.md):
  **원격 접속은 조건이 아니다**(보고서 4-3 폐기) — 원격 없이 4시간 406건.
  **끄기 명령의 경로도 방아쇠가 아니다** — 강제 339초 0건, 자연 560초 0건.
  **`Kernel-Power 566`이 화면 전원 전환의 OS 정식 기록**임을 발견(08-08부터 보존).
  폭풍은 566 꺼짐 구간과 정확히 겹치고, **강제 끄기는 566을 남기지 않는다**.
  단 566 꺼짐은 **필요조건이지 충분조건이 아니다**.
- [04 — AMD EDID 에뮬레이션이 실재하는가](issues/04-amd-edid-에뮬레이션.md):
  기능은 실재하고 계층상 `EDID_OVERRIDE`보다 상류다. **다만 가용성 판정은 틀렸다** —
  `EDIDManagement_Caps`가 `supported=1`을 돌려주지만 쓰기 경로는 Pro 전용이다(06이 정정).
  DSC는 제약이 아니다. AMD 릴리스 노트의 "EDID 에뮬레이션 + MSB 복귀 blank-out" 경고 확인.
- [05 — 관측자 효과](issues/05-관측자-효과.md): **기각.** 09-19 16:40 이전에 ETW 캡처가
  실행된 흔적이 하나도 없다. "캡처와 증상이 공존한 적 없다"는 이유는 억제가 아니라 **부재**다.
  09-18의 "무장"은 마지막 1010보다 1시간 뒤였다. 수동 집계기는 폭풍과 정면으로 공존했다.
- [06 — AMD EDID 에뮬레이션 검증 실험](issues/06-에뮬레이션-검증실험.md): **② 실행 불가 확정.**
  `ConnectionData_Set`·`EmulationMode_Set` 전 변형이 `-8 ADL_ERR_NOT_SUPPORTED`,
  AMD Software GUI 에도 항목 없음. 04의 가용성 근거를 정정한다.
- [07 — 증상은 설치 당일부터다](issues/07-증상-시작-시점.md) **★ 제출 핵심**:
  **09-08은 G80SH 설치일이다.** 설치 전 한 달 0건 → 설치 당일 21:59:49 첫 발생 → 11일간 4,775건.
  당일 비정상 종료 2회는 원인이 아니라 인식 불량과 씨름한 **결과**다.
  → "멀쩡하다 고장났다"가 아니라 **초기 불량**.
- [08 — 드라이버 완전 재설치](issues/08-드라이버-완전재설치.md): **기각.**
  AMD Factory Reset 으로 이전 모니터 EDID 잔재(`EDID_331_2701` ×2)와 `freesync_v1`
  저장값을 전부 지웠는데 **파형까지 동일하게 재발**했다(-42ms). 유령 devnode는 재생성됐다.
  부수: 재설치 중 생성된 devnode의 EDID가 **128바이트**(확장 블록 없음)였다.
- [09 — 야간 변수 전수 시험](issues/09-야간-변수-전수시험.md): **4종 전부 기각.**
  유령 devnode 제거 / OSD 공장 초기화(EDID 바이트 무변화) / 주사율 240→60Hz /
  FreeSync OFF(11분 15건, 파형 무변화 — 보고서 6-5 가설 폐기).
  **방법론 발견: 30분 절전에 0건이 나올 확률이 원래 45%다.** 상시규칙 3의 근거.
- [10 — 오디오 엔드포인트](issues/10-오디오-엔드포인트.md): **기각 — 오디오는 부산물이다.**
  GPU 오디오를 끈 채로 **09-20 14:33~14:40 에 시간당 113.9 로 재발**했다(기준선 77보다 높다).
  캡처에 `MMDEVAPI`/`HDAUDIO` 0건인데도 220(-38~-62ms)→1010 파형과 40~47초 주기가 그대로다.
  → 엔드포인트 진동은 확장 블록 불안정의 **지표**이지 재열거를 만드는 고리가 아니다.
  09-19 21:11 삼성 앱 초기화도 함께 기각. 다만 **발생률이 줄었는지는 미판정** — 조치 이후
  실제 절전 노출이 3.57시간뿐이다(밤사이 PC 종료).
  부수(유지): **FreeSync 가 EDID 프로파일을 바꾼다**(`7B0C` ↔ `7B0B`, OSD 스위치로 재현).
  보고서 8항의 "대기 EDID" 서술 폐기. 단 **프로파일 전환만이 원인은 아니다** —
  `7B0B`에 고정해도 같은 주기로 제거된다.
- [11 — GPU DP 포트 교체](issues/11-포트교체-효과.md) *(프런티어 — 09-20 막힘 풀림, 방아쇠 당겨짐)* **★ 유일하게 효과 확인**:
  09-09 22:13에 GPU DP 포트를 맞바꾼 뒤 **사흘 정지**(35.5시간 절전에 2건).
  케이블은 그대로였다. 08(드라이버 완전 재설치)은 즉시 재발했으므로,
  **PC 쪽 상태 초기화로는 안 되고 물리 링크를 새로 세워야 하며 그 효과도 며칠 만에 사라진다.**
  → 초기화되는 것은 모니터 쪽 상태다.

> **2026-09-20 19:20 — 제출 보류 해제.**
> [13 — 패널 보호 「자동 연결 해제」](issues/13-패널보호-자동연결해제.md)를 확인했고
> **[끄기]** 였다. 원인 가설은 기각. 대신 **제출 진술이 하나 늘었다** —
> 설명서상 화면보호기는 끌 수 없고(p34) 「자동 연결 해제」는 그 동작 중 입력을 끊을지만
> 고르는 스위치인데, **꺼 둔 상태에서도 입력이 40~50초 주기로 끊긴다.**
> 설정과 실제 동작이 어긋나므로 삼성이 자사 문서로 바로 검증할 수 있다.
>
> **19:35 사용자 확인으로 OSD 축이 닫혔다.**
> `VRR Control` = **항상 해제** (쓴 적 없음) → 변수 아님. FreeSync(=동작, 현재 `7B0C`)와 별개 항목이다.
> `DisplayPort 버전` = **2.1**, `DSC` = **사용**. **사용자 결정: 끄지 않는다.**
> 4K240 을 포기하는 조치이고, 제품 정규 사양이 그것이므로 타당한 결정이다.
> → 제출본은 "광고 사양 그대로 쓰는데 발생한다"로 진술을 바꿨다.
>
> **같이 정정**: 09-3(주사율 240→60Hz)의 "기각"은 근거가 약하다. 60Hz 828초 0건 /
> 240Hz 1,112초 0건으로 **양쪽 다 침묵한 구간**이었다. 30분 침묵은 원래 45% 확률이므로
> (상시규칙 3) 폭풍이 60Hz 에서도 돈다는 것을 보인 적이 없다. **DSC/대역폭 축은
> "기각"이 아니라 "미판정"이다.** 제출본에서 해당 줄을 뺐다.

> **2026-09-20 재개 후 즉시 마감.** 포트 교체가 기각되면서 맵이 닫히는 줄 알았으나,
> 그 과정에서 **케이블이 한 번도 검증된 적 없다**는 것이 드러났다(11 의 배제 논증에 구멍).
> [12 — DP 케이블 교차시험](issues/12-케이블-교차시험.md)로 빼서 같은 날 시험했고
> **기각됐다** — 실적 있는 케이블로 바꿔도 첫 절전에서 그대로 터진다.
> **프런티어가 다시 비었다. 도착지 재설정 결정이 남아 있다(1 또는 2).**

- [12 — DP 케이블 교차시험](issues/12-케이블-교차시험.md): **기각. 케이블이 아니다.**
  11 의 "케이블 불량 아님" 논증에 구멍이 있었다 — 09-09 교체 때 케이블이 모니터에 붙은 채
  같이 움직여 둘을 가르지 못했다. **케이블은 한 번도 검증된 적이 없었다.**
  17:41 에 G80SH 를 G50F 가 쓰던 케이블(11일간 276건 실적)로 갈아 물렸더니
  17:47:28 화면 꺼짐 → 17:48:48 첫 1010, 시간당 93. 한 시간 전 원래 케이블 구간(147.9)과
  주기·강등 패턴이 구별되지 않는다. 대역폭도 변수가 아니다(새 케이블로 4K240 그대로).
  **AUX 채널 가설도 함께 기각.** → 논증이 틀렸을 뿐 결론은 맞았고, 이제 근거가 붙었다.
  **제출에서 "케이블 바꿔보셨어요?"에 예라고 답할 수 있게 됐다**(대장 15항).

- [16 — 45~48초 타이머의 해부](issues/16-사이클-해부.md) *(09-23, ETL 53개·135 사이클)*:
  **"45~48초"는 하나의 주기가 아니라 재연결 후 하한 34초·주 봉우리 37초 + 약 9초 격자다**
  (균등 대조 p<0.0001). 한 사이클 = GPU 조회 3회 — **EDID 상실(`Default_Monitor`) → 0.23초 뒤
  연결 상실 → 3.569초(상수) 뒤 재연결.** Q1 직전 10011 은 131/135 에서 0건(02 재확인).
  09-22 부터 발생률 상승(81.5→136.9→155.8/h)은 수명이 첫 봉우리(37초)로 쏠린 결과.
  **주인(PC 폴링 vs 모니터 절전 타이머)은 못 갈랐다** — 810 스택 워크 캡처가 가른다(승인 대기).

- [17 — 재열거 호출 스택](issues/17-재열거-호출스택.md) *(09-23, xperf)* **★ 02 정정**:
  810 스택은 커널 작업자에서 끝나 쓸모없었다. 대신 DxgKrnl 스택에서 Q1 앞단이
  **AMD 의 `IndicateConnectorChange` → `DdiQueryConnectionChange`** 임을 찾았고, 페이로드상
  **AMD 가 G80SH(260)만 "분리→0.1ms 뒤 연결"로 보고**한다(G50F 256 은 같은 시각 연결 유지).
  02 의 "드라이버가 스스로 판단"은 틀렸다 — 10011 이 안 찍히는 통로로 연결 변경을 올리고 있었다.
  AMD 가 분리를 무엇으로 알았는지는 `amdkmdag` 심볼이 없어 PC 쪽 관측의 끝.
  관찰(표본 2): Q1 1.4초 전에 AMD 전 포트 일괄 보고. **사용자 질문 "AMD 설정으로 해결되나" → 아니다(근거 17).**

- [18 — 방아쇠는 AMD 전 포트 점검](issues/18-AMD점검-방아쇠.md) *(09-23~24, 사이클 40·점검 64)* **★**:
  **Q1 40건 전부 AMD 일괄 점검 1.384~1.416초 뒤.** 재연결 후 **≤31.7초 점검 22회는 전부 무사,
  ≥36.5초 점검 36회는 전부 폭풍** — 겹침 없음. 16 의 하한 34초·37초 봉우리·격자가 전부 설명된다.
  G50F 는 같은 점검 64회 모두 연결 유지. 남은 것: 1.39초 안의 일(HPD vs AUX 시간초과)·점검 주기의 주인 —
  AMD 심볼 없이는 PC 에서 못 본다.

- [19 — GPU 런타임 절전 사슬](issues/19-GPU-런타임절전-사슬.md) *(09-24, xperf ReadyThread 스택)* **★**:
  "AMD 점검" = **GPU 가 D3(Windows 유휴 타이머)에서 D0 로 깨어날 때의 전 포트 재감지 — 6/6.**
  깨우는 쪽은 상주 프로그램(HWiNFO ADL 조회 ×2, 화면 구성 조회하는 powershell·Logitech Options+).
  Q1 은 AMD **타이머 구동 스레드**가 복귀 0.66초 뒤부터 2~3ms 폴링하다 올린다 — 인터럽트 아님.
  G50F 는 같은 순환 6회 모두 연결. 원인은 모니터, **방아쇠 빈도는 GPU 런타임 절전 × 깨우는 프로그램.**

- [20 — 야간 자율 시험](issues/20-야간-자율시험.md) *(09-24 03~07시)* **★**: "09-22 두 배"는 **집계 착시**(Default_Monitor 합산).
  깨우는 프로그램 단독·전부 정지로는 못 멎는다(chrome 까지 깨움). 32초 경계는 **순번 아닌 시간(약 32.3초), AMD ULPS 값과 무관**.
  **GPU 를 D0 에 붙잡으면 30분 0건 → 풀자 27초 만에 재발(A-B-A 확정).** PP_GPUPowerDownEnabled=0 은 D3 를 못 막음.
  06:35 Windows 업데이트 재시작(KB5124010)으로 시험 1건 중단 — OS 빌드 변수 발생.

## Not yet specified

- **완화책 결정** *(09-24, 20 에서 파생)* — 화면 꺼진 동안 GPU 를 D0 에 두는 상주 도구(우회책). 시제품
  `tools\g80sh-keepawake.ps1`, 배포·전력 비용은 사용자 결정.
- **일자별 발생률 추이** *(09-23 등록)* — 작업 `G80SH Daily Rate`(사용자, 매일 23:55)가
  `_evidence\daily-rate.log` 에 누적한다. 09-23 = 시간당 155.4 (1,617건, 역대 최다).

- ~~**"절전 전 상태"의 정체**~~ → **[13 — 패널 보호 「자동 연결 해제」](issues/13-패널보호-자동연결해제.md)
  으로 티켓화됨 (09-20).** 공식 설명서 p35 에 **"화면 최적화 기능 사용 중에 외부입력을
  해제할 수 있습니다 / 끄기·켜기 / S32HG80\*S 모델만 지원"** 이라는 OSD 항목이 있다.
  추측하던 "펌웨어·패널 케어 동작"이 **사용자가 켜고 끌 수 있는 스위치로 실재한다.**
  아직 확인 안 했다. 03 이 "전환 순간이 아니라 상태"까지 좁힌 것과 맞물린다.
- **09-10~09-12 정지의 원인** — 11이 포트 교체와 시각을 맞췄지만, 09-09 22:30의 Windows
  업데이트(KB5124007/KB5126052)도 같은 구간에 있다. 둘을 가르지 못했다.
- **같은 부팅 세션 안에서 구간마다 갈린다** *(09-20 추가)* — 09-20 첫 절전 구간(7.9분)은
  시간당 113.9, 같은 세션 둘째 구간(113.9분)은 0건이다. **재부팅으로 링크를 새로 세워도
  첫 절전에서 즉시 터졌다** — 11의 "물리 링크 재수립이 며칠을 번다"와 나란히 놓고 봐야 한다
  (재부팅은 물리 연결을 바꾸지 않으므로 모순은 아니나, "링크 재훈련"만으로는 설명이 안 된다).
  구간별로 갈리는 변수가 무엇인지 모른다. 09의 "발현 지연이 날마다 다르다"와 같은 축일 수 있다.
- ~~**왜 하필 45~48초인가**~~ → [16](issues/16-사이클-해부.md)이 답했다(하한 34초 + 약 9초 격자).
  `Kernel-PnP 220`을 누가 얼마 간격으로 거는지는 캡처에 답이 있을 수 있다(미분석).
- **다른 PC 에 연결하면 어떻게 되는가** — 가장 결정적인 대조군이지만 장비가 필요하다.
  HDMI 전환(링크 계층이 다름)도 같은 성격의 미시도 항목.
- **내장 GPU 에 물리면 어떻게 되는가** *(09-20 추가)* — `AMD Radeon(TM) Graphics`(iGPU)가
  존재한다. 메인보드에 영상 출력이 있으면 **장비 없이** AMD 외장 드라이버/miniport 를
  통째로 우회한 대조가 된다. 02 가 지목한 주체가 miniport 이므로 결정적일 수 있다.
  메인보드 출력 유무 미확인.
- **`Kernel-PnP 220` 을 누가 거는가** *(09-20 격상, 09-23 16 이 시간 구조까지 답함 —
  캡처에 스택이 없어 호출자는 미확정. 위 810 스택 워크 항목으로 이어진다)* — 하드웨어가 전혀 필요 없는데
  아직 안 했다. ETL 원본 6건이 이미 있고, 220(=`IoInvalidateDeviceRelations`) 호출 스택을
  보면 45~48초 타이머의 주인이 나올 수 있다. 02 가 "AMD miniport"까지만 좁혀뒀다.
- **제출 문서 실제 작성** — 대장에 재료는 다 모였다. 도착지 재설정에서 2를 고르면
  이것이 새 맵의 본체가 된다.

## Out of scope

- **(b) 삼성 제출 근거 확정** — 이번 맵의 도착지가 아니다. 다만 **기록은 계속 쌓는다**:
  `_evidence\삼성제출_대장.md` (Notes 상시규칙 7). 도착지 재설정에서 2를 고르면 범위 안으로
  들어온다. 기존 자료: `Desktop\G80SH_삼성제출_20260918\`.
- **`EDID_OVERRIDE` 확장** — 보고서 9-1에서 원리상 불가로 논증 종료(열거 후 적용).
  04가 계층 분석으로 재확인했다.
- **측정 장치가 사라진 경위** — 사용자가 지운 것으로 확인. 따지지 않는다.
- **증상을 가리는 조치 일체** — Notes 상시규칙 1. 단 09-19 GPU 오디오 비활성화는
  사용자 요청으로 예외 진행했고 [10](issues/10-오디오-엔드포인트.md)에 우회책으로 명시했다.
- **09-13 펌웨어 가설** — 파일 다운로드 날짜만 보고 세웠다가 폐기. 펌웨어는 받기만 하고
  설치하지 않았으며(USB 없음) 이미 최신이었다.
