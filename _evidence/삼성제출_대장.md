# 삼성 제출 대장 — G80SH 장치 반복 인식 문제

**이 파일의 역할**: 원인 추적에서 나오는 모든 결과 중 **삼성 제출에 영향을 주는 것**을
그때그때 누적한다. 제출 문서 자체가 아니라, 제출 문서를 고칠 때 보는 대장이다.

**갱신 규칙**: 티켓이 하나 풀릴 때마다, 제출 진술을 바꾸는 내용이면 여기에 한 줄 추가한다.
(맵 `C:\dev\PC\.scratch\g80sh-1010\map.md` Notes 상시규칙 5)

**진행 중인 원인 추적**: `C:\dev\PC\.scratch\g80sh-1010\` (맵과 티켓)
제출 자체는 그 맵의 **범위 밖**이다. 여기는 기록만 쌓는 곳이고, 실제 제출 문서 작성은
따로 연다.

---

## 1. 기존 제출 자료 (2026-09-18 작성)

`C:\Users\hans1\Desktop\G80SH_삼성제출_20260918\`

| 파일 | 상태 |
|---|---|
| `접수용_증상설명문.txt` | **수정 필요** (아래 3항) |
| `증상설명서.md` | **수정 필요** |
| `방문용_A4요약.html` | **수정 필요** |
| `기존_분석보고서.md` | 참고자료. 4-3항·8항이 흔들림 |
| `첨부이미지\` | 3_간헐성.png 재작성 필요 |
| `로그\` | 09-18 21:16 기준. 갱신 필요 |

대상: Odyssey OLED G8 **G80SH** / LS32HG802SKXKR / S/N **HNBL700092**
PC: Windows 11 Pro 10.0.26200, **AMD Radeon RX 9070 XT** 드라이버 `32.0.31041.1004`
연결: DisplayPort 1.4 + DSC

---

## 2. 현재 수치 (2026-09-19 17:14 기준)

`Microsoft-Windows-Kernel-PnP/Device Management` 이벤트 ID **1010**, 09-08 이후.

**DISPLAY 장치 제거 총 4,517건**

| 장치 | 건수 | 비고 |
|---|---:|---|
| `SAM7B0C` | 3,088 | G80SH 정상 EDID — **최초 09-08 21:59:49, 최후 09-19 17:13:46** |
| `Default_Monitor` | 1,294 | EDID 미확정/읽기 실패 |
| `SAM79DF` | 131 | G50F (정상 동작 중인 대조군 모니터) |
| `SAM7B0B` | 2 | G80SH 대기 중 EDID |
| `LHC2701` | 1 | |
| `SAM7B11` | 1 | G80SH 대기 중 EDID |

**일자별**

```
09-08     18      09-14    161
09-09  1,076      09-15    749
09-10     16      09-16    793
09-11      3      09-17    655
09-12      1      09-18    611
09-13     20      09-19    414  (17:14 기준, 진행 중)
```

---

## 3. 제출 문서에서 **고쳐야 할 것**

### 3-1. 발생 횟수 — 진술을 갱신할 것

- 기존: "2,298회"
- **갱신: "9월 8일 이후 4,500회 이상 (2026-09-19 기준, 계속 증가 중)"**

### 3-2. 【중요】 "원격 접속 후" 조건을 **삭제**할 것

기존 문서는 발생 조건을 *원격 접속 후 + 절전*으로 진술한다. **이것은 틀렸다.**

2026-09-19, 크롬 원격 접속이 **한 번도 없는 상태**에서 4시간 동안 406건이 발생했다.

```
11:41:15  감시자 시작
12:26:13  화면 절전 진입
          12시 57 · 13시 101 · 14시 95 · 15시 100 · 16시 53   합 406건
16:30:33  화면 켜짐
16:31:25  마지막 1010
16:30:35  이날 유일한 원격 접속 (40초, 폭풍이 끝난 뒤)
```

→ **진술이 단순해진다: "화면이 절전에 들어가 있는 동안 발생한다."**
원격은 절전을 유발하는 흔한 경로였을 뿐 조건이 아니다.
근거: `RemoteDisplaySwitch.log` 09-19 전량 + 위 이벤트 로그

**이 정정은 유리하다.** 원격 접속이라는 특수 조건이 빠지면 삼성 쪽에서
"특수한 사용 환경 탓"이라고 돌릴 여지가 줄어든다.

### 3-3. 재부팅으로 해결되지 않음 — 추가할 것

재부팅 2회를 넘겨 재발했다.

```
09-18 17:36:13  증상 정지
09-18 22:36     재부팅              → 23시 3건, 09-19 01시 2건
09-19 02:18     종료
09-19 11:40:47  부팅
09-19 12:26:13  절전 진입 → 47초 뒤 폭풍 재개, 4시간 406건
```

### 3-4. 화면 끄는 방식과 무관 — 추가할 것

두 경로 모두에서 대조 실험을 했고, 발생 여부를 가르지 못했다.

```
12:26:13  자연 절전(전원 정책 타임아웃)  →  47초 뒤 폭풍 시작
16:45:19  강제 끄기(SC_MONITORPOWER)     →  339초까지 0건
16:53:17  자연 절전(같은 경로)           →  560초까지 0건
```

→ 방아쇠는 "끄는 동작"이 아니라 **"절전에 들어가기 전의 상태"**에 있다.
정체는 아직 추적 중이다.

### 3-5. 제3자 도구 무관 — 논증이 더 강해졌다

기존 문서 5항(제3자 도구가 원인이 아님)은 그대로 유효하고, 09-19 데이터로 더 강해졌다.
**폭풍 4시간 동안 `RemoteDisplaySwitch`의 화면 전환 동작이 0회**였다.
감시자는 `QueryDisplayConfig`(읽기 전용) 호출만 했고 구성을 바꾸지 않았다.

### 3-6. 기전 진술의 무게중심 — 재검토 필요

기존 보고서 8항은 대기 EDID(`SAM7B0B` `0x7B0B` / `SAM7B11` `0x7B11`)로의 제품코드 변동을
기전으로 지목했다. 그러나 1010 이벤트 기준으로는 **09-08 이후 전체 4,517건 중 각각 2건 / 1건**뿐이고,
지배적인 것은 `Default_Monitor` **1,294건**(EDID 읽기 실패/미확정)이다.

**주의**: `0x7B0B`/`0x7B11`은 CCD API 조회에서 관측한 값이고 1010 이벤트와 같은 축이 아니다.
두 축을 맞추는 작업이 끝나기 전에는 이 항목을 제출 문서에 반영하지 말 것.

### 3-7. 드라이버 로드 단계 미도달 — 기존 유지

`Kernel-PnP/Configuration` 400(구성됨)/410(시작됨)이 0건.
장치가 열거 단계에서 탈락하고 드라이버 로드까지 가지 못한다. (기존 추가분석 3항)

---

## 4. 09-19 신규 확보 — ETW 1차 자료

기존 제출 자료에는 없던 것. **삼성/AMD가 직접 열어볼 수 있는 커널 추적 원본**이다.

| 파일 | 내용 |
|---|---|
| `g80sh-baseline-0919.etl` | ETW 원본 3,014,656 bytes. 정상 전환 구간 |
| `g80sh-dxg-agg-baseline-0919.txt` | 사람이 읽는 요약 |
| `g80sh-dxg-baseline-0919.csv` | 이벤트 표 |

**정상 전환(17:08:52)의 파형** — 폭풍 구간과 비교할 대조군

```
-96 ~ -73ms   DxgKrnl 10011 (StatusChangeNotify)  20여 건
      -24ms   Kernel-PnP 220  Begin querying bus relations
                              for device PCI\VEN_1002&DEV_7550...   ← GPU
        0ms   Kernel-PnP 222  End querying bus relations
        0ms   Kernel-PnP 1010 DISPLAY\SAM7B0C\...UID256 제거
```

**확인된 사실: 재열거의 대상은 모니터가 아니라 GPU다.**
220/222가 걸리는 장치는 `PCI\VEN_1002&DEV_7550`(RX 9070 XT)이다.
즉 "GPU의 자식 장치 목록을 다시 묻는" 동작이 일어나고 그 결과로 모니터 devnode가 빠진다.

**아직 폭풍 구간의 파형은 확보하지 못했다.** 순환 버퍼 캡처가 상시 가동 중이며,
다음 폭풍이 잡히면 여기에 추가한다. 그것이 나와야 원인 방향(모니터 vs 드라이버)을
제출 문서에 쓸 수 있다.

---

## 5. 제출 시 첨부할 원자료 목록

`C:\dev\PC\_evidence\`

| 파일 | 내용 |
|---|---|
| `2026-09-18_1010_전체.csv` | 1010 이벤트 전량 |
| `2026-09-18_SAM7B0C_간격.csv` | 사이클 간격 |
| `2026-09-18_간격_히스토그램.csv` | 간격 분포 (37초 하한선) |
| `2026-09-18_일자별_주기.csv` | 일자별 평균 주기 |
| `2026-09-18_추가분석.md` | 09-18 분석 |
| `g80sh-evidence.log` | 15분 단위 집계 |
| `forced-off-test.log` | 강제 끄기 대조 실험 |
| `natural-sleep-test.log` | 자연 절전 대조 실험 |
| `g80sh-baseline-0919.etl` + 디코드 2종 | **ETW 커널 추적 원본** |
| `research_AMD_EDID_에뮬레이션.md` | AMD EDID 에뮬레이션 조사 (PC 쪽 대응 가능성) |

**주의**: `research_AMD_EDID_에뮬레이션.md`는 PC 쪽 회피 수단 조사다.
제출하면 삼성이 "PC에서 해결 가능하다"고 돌릴 빌미가 된다. **제출 시 제외할 것.**

---

## 6. 갱신 이력

- **2026-09-19 17:15** 대장 신설. 09-19 결과 반영 (3-1 ~ 3-7, 4항).

---

## 7. 【중대】 09-19 17:45 폭풍 캡처 — 제출 전략에 영향을 준다

폭풍 구간의 ETW 원자료를 확보했다. **결과가 삼성에 유리한 쪽이다. 반드시 감안할 것.**

원본: `g80sh-dxg-0919-1745-1.etl` (13,238,272 bytes, 115,296건)
디코드: `g80sh-dxg-agg-0919-1745-1.txt`, `g80sh-dxg-0919-1745-1.csv`

### 폭풍 사이클의 선후

```
  -42ms   Kernel-PnP 220   Begin querying bus relations
                           for device PCI\VEN_1002&DEV_7550...   ← GPU. 여기서 시작
   -1ms   DxgKrnl 10011    StatusChangeNotify  ← 220 이후에야
    0ms   Kernel-PnP 1010  DISPLAY\SAM7B0C 제거
```

두 폭풍 사이클 모두 **220보다 앞선 `DxgKrnl` 10011이 0건**이다.
정상 전환(17:08:52)에서는 10011이 20여 건 앞섰다. **정반대다.**

### 무엇을 뜻하는가

- 재열거를 개시한 주체는 **PC 쪽(AMD display miniport)**이다.
  `IoInvalidateDeviceRelations`를 드라이버가 스스로 걸었다.
- **모니터가 링크를 끊어 OS에 알린 것이 아니다.** 그 경우라면 정상 전환처럼
  `StatusChangeNotify`가 앞섰어야 한다.

### 제출에 어떤 영향인가 — 솔직하게

**이것은 "모니터 불량"이라는 진술을 약화시킨다.** 삼성 측이 "PC 그래픽 드라이버 문제"로
돌릴 근거가 된다. 숨기면 나중에 더 크게 깨진다.

다만 아직 삼성에 유리하다고 단정할 수도 없다:

1. 캡처는 **드라이버가 왜 그렇게 판단했는지**를 말해주지 않는다. 드라이버가 주기적으로
   링크/EDID를 읽다가 실패해 "자식이 사라졌다"고 결론 냈다면 뿌리는 여전히 모니터다.
2. `Default_Monitor`(EDID 읽기 실패) 1,294건이 이를 뒷받침하는 정황이다.
3. **G50F(`SAM79DF`)는 같은 GPU·같은 드라이버·같은 절전에서 131건뿐**이다.
   드라이버 일반의 문제라면 두 모니터가 비슷해야 한다.
4. 기존 보고서 5항의 "증상이 포트가 아니라 모니터를 따라 이동" 관찰이 그대로 유효하다.

**→ 제출 진술은 "모니터가 링크를 끊는다"가 아니라
   "이 모니터에서만 드라이버가 반복적으로 장치 소실로 판단한다"로 가야 한다.**
   전자는 이 캡처가 반증한다. 후자는 캡처와 모순되지 않고 대조군(G50F)이 뒷받침한다.

### 06 검증 실험이 이 항목을 확정하거나 뒤집는다

AMD EDID 에뮬레이션을 켰을 때 폭풍이 멎으면 "드라이버가 EDID를 못 읽어서 생기는 일"이
확정되고, 그러면 **모니터가 EDID를 제대로 내주지 않는다**는 진술이 성립한다.
멎지 않으면 다른 축을 찾아야 한다. **확정 전까지 이 항목을 제출 문서에 반영하지 말 것.**

---

## 8. Kernel-Power 566 — 새로 확보한 Windows 네이티브 근거

`System` 로그 / `Microsoft-Windows-Kernel-Power` / **이벤트 ID 566**.
화면 전원 전환을 남기며 **2026-08-08부터 보존**돼 있다.
제3자 도구(`CaseDisplay.log`)가 아닌 **Windows 자체 기록**이라 제출 가치가 높다.

폭풍과 정확히 겹친다:

```
09-18 09:27:55  566 꺼짐  →  09~14시 510건  →  15:04:30 켜짐  →  15시 26으로 급감
09-19 12:26:11  566 꺼짐  →  12~16시 406건  →  16:30:33 켜짐  →  16:31:25 마지막
09-19 17:40:59  566 꺼짐  →  17:44:43 폭풍 시작 (222초 뒤)
```

**"화면이 절전에 들어가 있는 동안에만 발생한다"를 Windows 자체 로그로 입증할 수 있다.**
3-2항의 조건 정정(원격 삭제)과 함께 쓰면 진술이 매우 단순해진다.

---

## 9. 갱신 이력

- **2026-09-19 17:15** 대장 신설. 09-19 결과 반영 (3-1 ~ 3-7, 4항).
- **2026-09-19 17:52** 폭풍 캡처 확보. 7항(제출 전략 영향), 8항(566) 추가.

---

## 10. 【최중요】 설치 당일부터 정상 동작한 적이 없다 — 초기 불량

09-19 확인. **제출 진술의 골격을 바꾼다.**

### 사실

- **2026-09-08이 G80SH를 새로 설치한 날이다.** 사용자 확인:
  "모니터를 새로 설치한 날인데, 설치했을 때 모니터가 인식이 바로 안 되었던 문제가 있었어.
   어떻게 다시 연결 성공했는지는 기억이 안 나."
- Kernel-PnP 로그는 **2026-08-08까지** 보존돼 있어 설치 이전을 확인할 수 있다.

```
08-08 ~ 09-07 (한 달)   SAM7B0C 1010 :  0건   ← 모니터 연결 전
09-08 21:59:49          SAM7B0C 첫 1010       ← 설치 당일
09-08 ~ 09-19 (11일)    SAM7B0C 1010 : 3,088건
```

- 그 한 달의 DISPLAY 1010 143건은 전부 다른 장치다(`SAM79DF` 대부분, `LHC2701`,
  `Default_Monitor` 2건). `LHC2701`은 G80SH 이전에 쓰던 모니터로 보인다.
- 설치 당일 비정상 종료 2회(`Kernel-Power 41`, 20:36:29 / 21:28:45, BugcheckCode=0).
  BSOD가 아니라 강제 전원 차단 또는 행. **인식 불량과 씨름한 정황과 일치한다.**

### 제출 진술은 이렇게 간다

> **설치 당일부터 한 번도 정상 동작한 적이 없다.**
> 설치 시점에 인식 불량이 있었고, 그 이후 11일간 장치 제거가 4,500회 이상 반복됐다.
> 같은 PC · 같은 그래픽카드 · 같은 드라이버에 연결된 다른 모니터(Odyssey G50F)는
> 같은 기간 131건뿐이다. 발생은 화면이 절전에 들어가 있는 동안에만 일어난다.

**"멀쩡하다 고장났다"가 아니라 「초기 불량」이다.** 교환 사유로 훨씬 명확하다.

### 이것이 7항(드라이버 선행)의 약점을 덮는다

7항에서 "재열거를 개시한 주체가 AMD 드라이버"라는 캡처 결과가 삼성 측 반박 재료가
된다고 적었다. 10항이 그 반박을 막는다:

- 드라이버는 **08-17자로 설치돼 있었고 한 달간 G50F를 문제없이 다뤘다.**
- 09-08에 바뀐 것은 **이 모니터가 연결된 것 하나뿐**이다.
- 즉 드라이버 일반의 문제가 아니라, **이 드라이버가 이 모니터를 다룰 때만 생기는 일**이다.

→ 7항의 권고("이 모니터에서만 드라이버가 반복적으로 장치 소실로 판단한다")를
   10항이 로그로 뒷받침한다. 두 항을 함께 쓸 것.

### 3-1항 수치 진술 보강

- 기존: "9월 8일 이후 4,500회 이상"
- **보강: "설치일(9월 8일) 이후 11일간 4,500회 이상. 설치 이전 한 달간 0회."**
  뒷부분이 핵심이다. 로그 보존 범위가 08-08까지라는 점을 함께 명시할 것.

---

## 11. 갱신 이력 (9항 대체)

- **2026-09-19 17:15** 대장 신설. 09-19 결과 반영 (3-1 ~ 3-7, 4항).
- **2026-09-19 17:52** 폭풍 캡처 확보. 7항(제출 전략 영향), 8항(566) 추가.
- **2026-09-19 18:35** 06 종료(AMD EDID 에뮬레이션 실행 불가 — ADL 쓰기 `-8`, GUI 항목 없음).
  **10항 추가: 설치 당일부터 불량 = 초기 불량.** 제출 골격이 바뀐다.

---

## 12. 09-19 야간 — 소프트웨어 계층 전수 배제 완료

### 12-1. 펌웨어가 최신인데도 발생한다 【중요】

```
OSD 정보 메뉴 : M-H800GGAA-1002.2   (최신 버전)
```

삼성 측의 두 가지 상투적 대응을 미리 차단한다:

- "펌웨어를 업데이트하세요" → **이미 최신**
- "구버전의 알려진 문제입니다" → **최신에서 발생**

### 12-2. 배제된 조치 전수 목록

| 계층 | 조치 | 결과 |
|---|---|---|
| Windows | `EDID_OVERRIDE` | 원리상 불가 (열거 후 적용) |
| AMD 드라이버 | EDID 에뮬레이션 | 실행 불가 (ADL 쓰기 전부 `-8`, GUI 항목 없음) |
| AMD 드라이버 | **완전 재설치 (Factory Reset)** | **무효** — 파형까지 동일(-42ms) |
| AMD 드라이버 | 이전 모니터 EDID 잔재 제거 | 무효 |
| Windows PnP | 유령 devnode 제거 | 무효 |
| 모니터 설정 | **OSD 공장 초기화** | **무효** — EDID 바이트 무변화 |
| 모니터 설정 | 삼성 디스플레이 앱 설정 초기화 | 무효 |
| 링크 | 주사율 240→60Hz (DSC 회피) | 무효 |
| 링크 | **FreeSync / VRR 끄기** | **무효** — 11분 15건, 파형 무변화 |
| 모니터 펌웨어 | 최신 `M-H800GGAA-1002.2` | **최신에서 발생** |

**소프트웨어·설정 계층이 전부 배제됐다. 남는 것은 하드웨어다.**

### 12-3. 사용자가 겪는 "장치 연결/해제음"의 정체를 규명했다

모니터 스피커(DP 오디오 엔드포인트)가 붙었다 떨어졌다를 반복한다.

```
20:58:46.930  SWD\MMDEVAPI\{...} 구성됨   이름 = "1 - Odyssey G80SH"
20:58:48.185  제거                        ← 1.3초 만에
20:59:46.097  제거
```

DP 오디오 엔드포인트는 **EDID 의 CTA-861 확장 블록**에서 파생된다.
→ **모니터가 EDID 확장 블록을 안정적으로 내주지 못한다는 직접 증거**이며,
   소리와 1010 반복이 같은 원인임을 보여준다.

보강 증거: 드라이버 재설치 중 생성된 devnode 의 EDID 가 **128바이트**였다
(정상 384바이트 = base 128 + 확장 256). 확장 블록을 못 읽은 것이다.

### 12-4. EDID 프로파일 이중화의 실체 — 보고서 8항 정정

기존 보고서 8항은 "대기 상태에서 제품 코드가 `7B0B`/`7B11`로 바뀐다"고 봤다. **정정한다.**

```
FreeSync ON    SAM 0x7B0C
FreeSync OFF   SAM 0x7B0B     ← OSD 스위치 하나로 재현 확인
```

스위치는 대기 상태가 아니라 **VRR(FreeSync) 상태**다. 그리고 절전 중 폭풍에서 두 가지가
모두 관측된다:

```
프로파일 간 교대   21:08:41 SAM7B0B → 21:09:13 SAM7B0C → 21:09:40 SAM7B0B
프로파일 내 반복   20:53~20:58  SAM7B0B 만 반복 제거 (FreeSync OFF 고정)
```

→ **제품 코드 전환만이 원인은 아니다.** 어느 프로파일에 고정해도 같은 주기로 제거된다.
   이 항목은 제출 문서에 쓸 때 기존 8항 서술을 반드시 고쳐야 한다.

### 12-5. 제출 진술 최종형

> Odyssey OLED G8(G80SH)을 2026-09-08 설치한 당일부터, 화면이 절전에 들어가 있는 동안
> 약 45초마다 모니터가 장치 목록에서 제거되는 현상이 반복된다. 설치 이전 한 달간 동일 항목
> 0건, 설치 후 11일간 4,700건 이상이다. 같은 PC·같은 그래픽카드·같은 드라이버에 연결된
> 다른 삼성 모니터(Odyssey G50F)는 같은 기간 276건에 그친다.
>
> 원인을 PC 쪽에서 찾기 위해 그래픽 드라이버 완전 재설치, 드라이버 저장값 초기화,
> 장치 목록 정리, 주사율·FreeSync 변경, 모니터 OSD 공장 초기화, 삼성 디스플레이 앱
> 설정 초기화를 모두 시행했으나 증상이 동일하게 재현된다. 모니터 펌웨어는 최신
> (M-H800GGAA-1002.2)이다.
>
> 커널 추적(ETW) 결과, 모니터가 연결을 끊어 알리는 것이 아니라 그래픽 드라이버가
> 모니터 정보(EDID)를 정상적으로 읽지 못해 장치가 사라진 것으로 판단하는 형태다.
> 모니터 스피커 장치도 같은 주기로 붙었다 떨어졌다를 반복하며, 이는 모니터가 내보내는
> 정보의 확장 영역이 불안정함을 보여준다.

첨부: ETW 원본 4건, 이벤트 로그 전량, 대조 실험 로그 일체.

---

## 14. 오디오 스택을 통째로 꺼도 재발한다 (2026-09-20) — 12-3 보강

**제출 진술을 강화하는 결과다.** 12-3 은 모니터 스피커가 같은 주기로 붙었다 떨어지는 것을
확장 블록 불안정의 직접 증거로 들었다. 그 관찰은 유지되되, **인과 방향이 확정됐다.**

```
09-19 21:15:49   GPU(9070 XT) DP/HDMI 오디오 장치 전체를 사용 안 함으로 전환
                 HDAUDIO\...VEN_1002&DEV_AA01...REV_100A   Status = Error
09-20 14:31:27   재부팅 (오디오 비활성 상태 유지됨 — 재확인)
09-20 14:32:54   화면 꺼짐
09-20 14:33:27   폭풍 시작.  1010 15건 / 7.9분 = 시간당 113.9
```

당일 캡처 `g80sh-dxg-0920-1434-1.etl` / `...agg-0920-1434-1.txt` (14:31:35~14:36:21):

```
오디오 이벤트          MMDEVAPI / audioendpoint / HDAUDIO   0건
Kernel-PnP 220 → 1010  -41ms / -62ms / -38ms                  ← 09-19 캡처의 -42ms 와 동일
1010 주기              46.9 / 39.8 / 40.0 / 43.0초            ← 동일
Default_Monitor 강등   재현                                    ← 동일
```

**제출에 쓸 문장**: 모니터 스피커 장치가 반복해서 붙었다 떨어지는 현상은 이 문제의 *증상*이며,
PC 쪽에서 해당 오디오 장치를 완전히 사용 중지시켜도 **모니터가 사라지는 현상은 속도·주기·
내부 이벤트 순서까지 동일하게 반복된다.** 즉 PC 의 오디오 처리 계층은 원인이 아니다.

**함께 기각**: 09-19 21:11 에 시행한 삼성 디스플레이 앱 설정 초기화도 효과가 없음이 확인됐다.
12-5 제출 진술의 "삼성 디스플레이 앱 설정 초기화를 모두 시행했으나 증상이 동일하게 재현된다"에
**직접 증거가 붙었다**(그전까지는 오디오 비활성과 4분 차이로 겹쳐 가를 수 없었다).

**단서**: 조치 이후 실제 절전 노출이 3.57시간뿐이라 "발생률이 줄었는가"는 아직 답하지 않았다.
**재발 사실만으로 충분하므로 제출에는 영향이 없다.**

---

## 17. 설정과 실제 동작의 불일치 — 패널 보호 「자동 연결 해제」 (2026-09-20)

**공식 설명서(BN81-29068D-02)를 읽고 나온 새 제출 근거.** 추론이 아니라
**삼성 자사 문서와 대조해서 나온 불일치**이므로 반박하기 어렵다.

```
p35  패널 보호 > 자동 연결 해제
     "화면 최적화 기능 사용 중에 외부입력을 해제할 수 있습니다. 끄기 / 켜기"
     "S27HG80*S / S32HG80*S 모델만 지원합니다"      ← 대상 모델이 정확히 해당
     현재 값 = [끄기]  (2026-09-20 사용자 확인)

p34  "OLED 제품은 10분 동안 같은 정지 영상이 계속 표시되면 화면보호기 기능이
      자동으로 실행되고, 픽셀을 보호하기 위해 화면보호기 기능은 끌 수 없습니다"
```

→ 패널 보호 동작 자체는 끌 수 없고 계속 돈다.
→ 「자동 연결 해제」는 그 동작 중 **입력을 끊을지 말지**만 고르는 스위치다.
→ **끄기인데도 40~50초 주기로 입력이 반복 해제된다.**

**제출 문장**

> 사용설명서 35쪽의 「패널 보호 > 자동 연결 해제」를 [끄기]로 설정해 두었음에도,
> 화면 절전 구간에서 외부입력이 40~50초 주기로 반복 해제됩니다. 34쪽에 따르면
> 화면보호기 동작 자체는 끌 수 없으므로, 해당 설정은 그 동작 중 입력 해제 여부만을
> 결정합니다. 설정과 실제 동작이 일치하지 않습니다.

커널 추적 결과(7항)와 맞물린다 — 모니터가 해제를 정식으로 알리는 신호(HPD/
StatusChangeNotify)가 **한 건도 선행하지 않는다.** 정상 경로의 해제가 아니다.

### 가설로서는 기각됐다

원래는 "이 기능이 켜져 있어서 생기는 일 아닌가"를 의심했고, 켜져 있었다면
**초기 불량 주장이 통째로 무너질 뻔했다.** 확인 결과 끄기였으므로 기각이고,
동시에 위 불일치 진술이 생겼다. (티켓 13)

### OSD 축 정리 (2026-09-20 19:35 사용자 확인)

```
VRR Control        항상 [해제]. 쓴 적 없음 → 변수 아님
                   FreeSync(현재 [동작], devnode 7B0C)와 별개 항목이다
DisplayPort 버전   2.1
DisplayPort DSC    사용
                   → 사용자 결정: 끄지 않는다. 4K240 을 포기하는 조치다
```

**제출 진술을 바꿨다**: "아직 안 해봤다"가 아니라
**"제품이 광고하는 사양(DP 2.1 / 4K 240Hz / DSC) 그대로 쓰는데 발생한다.
사양대로 못 쓰게 하는 조치는 해결이 아니다."** 이쪽이 더 강하다.

### 【정정】 09-3 주사율 시험은 "기각"이 아니라 "미판정"이다

제출본 표에 **"주사율 240→60Hz → 동일 재현"**으로 적었는데 **근거가 없다.**

```
60Hz    828초  → 0건
240Hz 1,112초  → 0건, 이후 재발
```

**양쪽 다 침묵한 구간이었다.** 폭풍이 60Hz 에서도 돈다는 것을 보인 적이 없다.
30분 침묵은 원래 45% 확률이므로(상시규칙 3) 이것으로는 판정할 수 없다.
티켓 09 의 "240Hz 가 더 오래 버텼다"는 논증은 서지만 **제출에 쓸 강도가 아니다.**

→ **제출본에서 해당 줄을 삭제했다.** DSC/대역폭 축은 미판정으로 남는다.
   다만 제품 정규 사양에서 발생한다는 사실은 그대로이므로 주장에 영향 없다.

---

## 16. 【정정】 G80SH 에는 내장 스피커가 없다 (2026-09-20)

**제출 문서에 "EDID 는 오디오를 선언하는데 OSD 에 사운드 설정이 없다 = 불일치"로
쓰려다 철회했다. 틀린 지적이다.**

G80SH 공식 사양의 연결 단자는 다음이 전부다
([TFTCentral 출시 사양](https://tftcentral.co.uk/news/samsung-odyssey-g80sh-officially-launched-a-new-32-4k-penta-tandem-qd-oled-monitor)):

```
DisplayPort 2.1 (UHBR20) 1 / HDMI 2.1 2 / USB-C(98W) 1 / USB-A 2 / 헤드폰 단자 1
```

**스피커 항목이 없다.** 따라서 OSD 에 음량 설정이 없는 것은 정상이고,
EDID 가 `LPCM 2채널`을 선언한 것은 **헤드폰 단자로 나가는 출력 경로** 때문이지
스피커가 있어서가 아니다.

### 대장·티켓의 기존 서술 정정

- 12-3 항과 14 항의 **"모니터 스피커"** 는 정확히는 **"모니터의 오디오 출력 엔드포인트
  (헤드폰 단자 경로)"** 다. 기전 논증 자체는 영향 없다 — 오디오 정보가 EDID **확장 블록**에서
  파생된다는 점, 그 엔드포인트가 같은 주기로 진동한다는 점은 그대로다.
- 티켓 10 의 "모니터 스피커는 기능이 막혀 있어 잃는 것도 없다"는, 실은 **스피커가 없어서**
  잃을 것이 없었던 것이다.

**제출 진술에 미치는 영향**: 12-5 최종형의 "모니터 스피커 장치도 같은 주기로 붙었다
떨어졌다를 반복하며" → **"모니터의 오디오 출력 장치도"** 로 바꿔 쓸 것. 제출본에는 반영됐다.

---

## 15. 케이블을 바꿔도 동일하게 재현된다 (2026-09-20) — 5장 논증 보강

**제출 전 반드시 메워야 했던 구멍이었다.** 기존 자료는 "케이블·포트·GPU 출력이 원인이
아님을 확정한다"(보고서 5장)고 적었으나, 근거는 09-09 의 GPU 포트 교체 하나뿐이었다.
그때 **케이블은 모니터에 붙은 채 같이 움직였으므로** 케이블과 모니터를 가르지 못한다.
즉 **케이블은 이 추적에서 한 번도 검증된 적이 없었다.**

상담 창구의 첫 질문이 "케이블 바꿔보셨습니까"이므로, 그대로 제출하면 즉시 되돌아왔을 것이다.

### 시험

```
17:41:15  G80SH 의 DP 케이블을 교체
          새 케이블 = G50F 가 쓰던 것. 같은 PC·같은 GPU 에서 11일간 276건의 실적
17:45:13  G80SH 정상 부착. 3840x2160 @240Hz — 네이티브 그대로
17:47:28  화면 꺼짐
17:48:48  첫 1010. 이후 61.4초 / 43.0초 간격으로 반복
```

### 같은 날 같은 포트 직접 대조 (한 시간 차이)

```
구간             케이블                 첫 발생까지   시간당    주기
17:21:10 꺼짐    원래 G80SH 케이블        33초        147.9    37.6~52.6초
17:47:28 꺼짐    G50F 가 쓰던 케이블      81초         93      43.0~61.4초
```

둘 다 기준선(시간당 77~148) 안이고 **주기와 `Default_Monitor` 강등 패턴이 구별되지 않는다.**

### 제출에 쓸 문장

> 정상 동작 중인 다른 DP 케이블(동일 PC 의 다른 삼성 모니터에서 11일간 문제없이 사용)로
> 교체해도 증상이 동일하게 재현됩니다. 교체 후 첫 절전에서 81초 만에 재발했고, 반복 주기
> (약 40~60초)와 이벤트 순서가 교체 전과 구별되지 않습니다. 교체한 케이블로도
> 3840x2160 240Hz 가 정상 표시되므로 대역폭 문제도 아닙니다.

**보고서 5장·254행의 「케이블·포트·GPU 출력 배제」는 이것으로 근거가 완성됐다.**
(기존 서술은 논증이 틀렸을 뿐 결론은 맞았다.)

---

## 18. 반복 주기의 구조 — 타이머 구동이다 (2026-09-23, 티켓 16)

ETW 원본 53개에서 폭풍 135 사이클을 분해했다(`tools\g80sh-cycle.py`).

### 사실

```
한 사이클
  ① 모니터가 연결된 채로 EDID 를 잃는다 (장치가 Default_Monitor 로 바뀜)
       이 직전에 링크 상태 변경 통지 없음: 131/135 사이클
  ② 0.23초 뒤 연결이 끊긴다
  ③ 3.569초 뒤 재연결 (흔들림 ±0.03초)

재연결 -> 다음 ①
  최소 34.2초, 주 봉우리 약 37초, 이후 약 9초 간격(45, 55초 …)에 몰린다
  (격자 검정, 균등분포 대조 p < 0.0001)

발생률  09-21 시간당 81.5 -> 09-22 136.9 -> 09-23 155.8
        같은 기간 재연결 후 수명 중앙값 47.6 -> 37.7 -> 37.6초
```

### 제출에 쓸 문장

> 반복은 무작위가 아닙니다. 모니터가 다시 인식된 뒤 **최소 34초, 대부분 약 37초** 만에,
> 그때 넘기면 **약 9초 간격**으로 다음 해제가 일어납니다. 해제 때는 먼저 EDID 를 읽을 수 없게
> 되고(연결은 유지), 0.2초 뒤 연결이 끊기며, **정확히 3.57초 뒤** 다시 연결됩니다.
> 이 시간 구조가 DisplayPort 2.1/1.1, DSC 사용/해제, FreeSync 사용/해제, 케이블·포트 교체에서
> 모두 동일합니다. 절전 중 모니터 펌웨어의 타이머 값과 대조해 주십시오.
> 09-22 부터는 첫 37초 시점에서 해제되는 비율이 늘어 발생 빈도가 약 두 배가 됐습니다.

**주의**: 이 구조만으로는 타이머가 모니터 쪽인지 GPU 드라이버 쪽인지 가를 수 없다.
진술은 "구조를 대조해 달라"에서 멈춘다. 7항(드라이버 선행)의 약점은 그대로 남아 있고,
07·08·12·14·15 항의 배제 근거가 모니터 쪽을 가리키는 주 근거다.

---

## 19. 【7항 정정】 GPU 드라이버는 "이 모니터가 분리됐다가 다시 연결됐다"고 보고하고 있다 (2026-09-23, 티켓 17)

7항은 "220 앞에 StatusChangeNotify 가 없으니 **드라이버가 스스로 판단했다**"고 적었다.
**틀렸다.** 호출 스택을 붙인 캡처(xperf, DxgKrnl)로 보니 드라이버는 다른 통로
(`DxgkCbIndicateConnectorChange` → `DdiQueryConnectionChange`)로 **연결 변경을 보고**하고 있었다.

```
매 사이클 첫 해제 직전, GPU 드라이버가 Windows 에 보고한 내용
  G80SH (대상 260)   모니터 분리  → 0.1ms 뒤 → 모니터 연결
  G50F  (대상 256)   같은 시각 전 포트 일괄 보고에서 계속 "연결"
```

### 제출에 쓸 문장

> 커널 추적(호출 스택 포함)으로 확인한 결과, 매 반복마다 그래픽 드라이버가 Windows 에
> **"G80SH 가 분리되었다가 즉시 다시 연결되었다"**는 연결 상태 변경을 보고합니다.
> 같은 그래픽카드·같은 드라이버에 연결된 다른 삼성 모니터(G50F)는 같은 시각에 계속
> "연결"로 보고됩니다. PC 는 모니터에서 들어온 연결 상태 변화를 전달할 뿐이며,
> 변화의 원천은 G80SH 쪽입니다.

**7항의 약점("PC 쪽이 먼저 움직인다")이 해소됐다.** 7항의 제출 보류 주의문도 이것으로 대체한다.

**주의**: 드라이버가 "분리"를 무엇으로 감지했는지(HPD 신호 / AUX 응답 실패)는 드라이버 심볼이
없어 PC 에서 더 볼 수 없다. 표본 2개에서 **드라이버의 전 포트 일괄 점검 1.4초 뒤에** 발생했다 —
확정되면 "드라이버 점검에 모니터가 절전 중 잘못 응답한다"가 된다. 확정 전에는 제출에 쓰지 말 것.

---

## 20. 재현 조건 — 링크 수립 약 32~36초 뒤부터, 그래픽 드라이버의 정기 점검마다 1.39초 뒤 분리 (2026-09-24, 티켓 18)

사이클 40개·드라이버 점검 64개를 커널 추적으로 대조했다.

```
그래픽 드라이버의 전 포트 점검 64회 — 점검 순간 G80SH 는 64회 모두 "연결"
  모니터 재연결 후 25~32초에 온 점검 22회   →  전부 무사
  모니터 재연결 후 36~63초에 온 점검 36회   →  전부 1.384~1.416초 뒤 G80SH 분리 (예외 0)
같은 점검을 받은 G50F                       →  64회 모두 연결 유지
```

### 제출에 쓸 문장

> 그래픽 드라이버는 연결된 모든 모니터의 상태를 주기적으로 점검합니다. G80SH 는 다시 연결된 뒤
> **약 32초까지는 이 점검을 정상적으로 넘기지만, 약 36초 이후에는 점검이 올 때마다 정확히
> 1.39초(±0.02초) 뒤 연결이 끊깁니다.** 점검 58회 중 예외가 없었습니다.
> 같은 점검을 받는 다른 삼성 모니터(G50F)는 한 번도 끊기지 않았습니다.
> 화면 절전 상태에서 링크 수립 약 32~36초 뒤 모니터 내부 상태가 바뀌는 시점을 확인해 주십시오.
> DP 소스에서 주기적으로 싱크를 점검하면 벤치에서 재현될 것으로 봅니다.

**18항(9초 격자)은 이것의 결과였다** — 수명 하한 34초 = 경계 약 32.8초 + 1.39초.

---

## 21. 발생 사슬 — GPU 가 절전에서 깨어날 때마다 G80SH 만 떨어진다 (2026-09-24, 티켓 19)

20항의 "그래픽 드라이버의 정기 점검"은 **GPU 가 런타임 절전(D3)에서 깨어날 때 하는 표준 재감지**였다.
화면이 꺼지면 Windows 가 GPU 를 D3 로 내리고, 상주 프로그램이 GPU 를 건드리면 다시 깨운다.

```
150초 캡처, 재감지 6회
  GPU D3 → D0 복귀 → 약 60ms 뒤 전 포트 재감지 : 6/6
  재감지 순간 G80SH·G50F 모두 "연결"
  그 뒤 G80SH 만 "분리" 보고 (재연결 32초 이후일 때) / G50F 는 0회
```

### 제출에 쓸 문장

> 모니터가 절전 중일 때 그래픽카드도 Windows 표준 런타임 전원 관리에 따라 절전과 복귀를
> 반복하며, 복귀할 때마다 연결된 모니터를 다시 감지합니다(DisplayPort 표준 동작).
> 같은 그래픽카드에 연결된 G50F 는 이 재감지를 모두 정상 통과하지만, G80SH 는 절전 진입
> 약 32초 이후부터 재감지 1.4초 뒤 연결이 끊어집니다.
> 소스 측 전원 복귀 후의 재감지(AUX/HPD 처리)에서 G80SH 의 응답을 확인해 주십시오.

**주의 — 제출에서 빼거나 조심할 것**: 사슬상 GPU 를 깨우는 것이 HWiNFO 등 상주 프로그램이라는
사실은 삼성이 "제3자 프로그램 탓"으로 돌릴 빌미가 된다. 그러나 (1) G50F 가 같은 조건을 전부 통과했고
(2) GPU 복귀·재감지는 Windows·DP 표준 동작이며 어떤 프로그램이든 일으킬 수 있다(화면 구성 조회만으로도
깨어났다 — Logitech Options+, Windows 스크립트). 이 두 점을 함께 쓸 것.

---

## 22. 정정과 확정 (2026-09-24 야간, 티켓 20)

### 22-1. 【정정】 "09-22 부터 발생 빈도 두 배"는 집계 착시였다 — 18항·20항의 해당 문장을 뺄 것

1010 합산에 Default_Monitor 제거가 섞여 있었고, 09-20 17:04 이후 거의 모든 사이클이 Default_Monitor 를 거쳐
사이클당 2건이 됐다. **사이클(SAM7B0C 제거) 빈도는 09-15 이후 시간당 약 55~80으로 평탄하다.**
제출에는 사이클 빈도를 쓴다.

### 22-2. 32초 경계는 PC 설정으로 움직이지 않는다

재감지 191회: 재연결 후 **32.1초까지 온 재감지는 전부 무사, 32.5초부터는 전부 분리**(약 0.4초 폭).
AMD 드라이버의 ULPS 진입 지연을 30초→90초로 바꾸고 GPU 를 재시작해도 경계는 그대로였다.
→ 경계는 **모니터 쪽 고정 타이머**로 보는 것이 가장 자연스럽다.

### 22-3. GPU 재감지가 없으면 증상도 없다 (A-B-A)

GPU 에 가벼운 작업을 계속 줘 절전 진입을 막은 30분 동안 **분리 0건**, 작업을 멈추자 **27초 만에 재발**.

### 제출에 쓸 문장

> 화면이 꺼진 동안 그래픽카드가 표준 전원 관리로 절전·복귀하며 모니터를 다시 감지할 때만 증상이 납니다
> (복귀를 막으면 30분간 0건, 풀면 27초 만에 재발). G80SH 는 연결 후 약 32.3초가 지나면 이 재감지에
> 1.4초 뒤 연결을 끊으며, 이 경계는 PC 쪽 설정을 바꿔도 움직이지 않았습니다. 같은 조건의 G50F 는
> 재감지 수백 회를 모두 정상 통과했습니다. 연결 후 약 32초에 동작하는 모니터 절전 단계와 그 상태에서의
> DP 재감지(AUX/HPD) 응답을 확인해 주십시오.

---

## 13. 갱신 이력 (11항 대체)

- **2026-09-19 17:15** 대장 신설. 3-1~3-7, 4항.
- **2026-09-19 17:52** 폭풍 캡처. 7항(제출 전략 영향), 8항(566).
- **2026-09-19 18:35** 06 종료. 10항(초기 불량).
- **2026-09-19 21:50** 12항 추가 — 펌웨어 최신 확인, 소프트웨어 계층 전수 배제,
  오디오 엔드포인트 기전 규명, 보고서 8항 정정, 제출 진술 최종형.
- **2026-09-20 17:05** 14항 추가 — 티켓 10 기각. GPU 오디오 전체 비활성 + 재부팅 상태에서
  09-20 14:33 재발(시간당 113.9), 파형·주기 동일. 오디오 계층과 삼성 앱 초기화 둘 다 배제.
  ETW 원본 1건 추가 확보(`g80sh-dxg-0920-1434-1.etl`, 총 5건).
- **2026-09-20 19:20** 17항 추가 — 공식 설명서 대조. 「패널 보호 > 자동 연결 해제」가
  [끄기]인데도 입력이 반복 해제된다. **설정·동작 불일치**라는 새 제출 근거.
  티켓 13 은 원인 가설로는 기각(켜져 있었다면 초기 불량 주장이 무너질 뻔했다).
  제출 보류 해제. 제출본에 3번 문서(OSD 설정 확인기록) 추가.
- **2026-09-20 18:20** 16항 추가 — **정정**: G80SH 에 내장 스피커가 없다(공식 사양 연결단자는
  헤드폰 단자뿐). "EDID 선언 vs OSD 불일치" 지적은 틀렸으므로 제출본에서 철회했다.
  "모니터 스피커" → "모니터의 오디오 출력 장치"로 표현 통일.
  **제출 패키지 작성** — `C:\Users\hans1\Desktop\G80SH_삼성제출_20260920\` (36MB)
- **2026-09-20 17:55** 15항 추가 — 티켓 12 기각. **케이블 배제의 근거가 비로소 생겼다.**
  기존 5장 논증은 09-09 포트 교체 하나에 기댔는데 그때 케이블이 모니터와 함께 움직여
  둘을 가르지 못했다(논증 구멍). 17:41 에 실적 있는 케이블로 교체 → 17:48:48 재발,
  시간당 93, 주기·강등 패턴 동일. 대역폭·AUX 채널 가설도 함께 기각.
  같은 날 티켓 11(GPU DP 포트 교체)도 기각 — 09-09 의 사흘 정지가 재현되지 않았다.
  ETW 원본 2건 추가(`0920-1704-2`, `0920-1737-3`, 총 6건).
- **2026-09-23 22:10** 18항 추가 — 티켓 16. 반복 주기의 구조(재연결 후 하한 34초·약 37초 + 약 9초 격자,
  EDID 상실 → 0.23초 → 연결 상실 → 3.569초 → 재연결). 09-22 부터 발생률 약 두 배. ETW 원본 53개.
- **2026-09-23 22:55** 19항 추가 — 티켓 17. **7항 정정**: 드라이버는 스스로 판단한 것이 아니라
  ConnectorChange 통로로 "G80SH 분리→즉시 연결"을 보고한다(G50F 는 같은 시각 연결 유지). xperf 스택 캡처.
- **2026-09-24 00:30** 20항 추가 — 티켓 18. 재현 조건 확정: 재연결 후 ≤31.7초 점검 22회 무사 / ≥36.5초 점검 36회
  전부 1.39초 뒤 분리. 18항의 격자를 설명한다.
- **2026-09-24 03:10** 21항 추가 — 티켓 19. "정기 점검" = GPU 런타임 절전(D3) 복귀 재감지(6/6).
  Q1 은 AMD 타이머 스레드가 올린다(인터럽트 아님). 깨우는 쪽 = 상주 프로그램 — 제출 시 표현 주의.
- **2026-09-24 06:55** 22항 추가 — 티켓 20. 두 배 착시 정정, 32초 경계는 PC 설정으로 불변(ULPS 90초),
  GPU 절전 복귀를 막으면 0건(A-B-A). 06:35 Windows 업데이트 재시작(KB5124010)으로 시험 1건 중단.
- **2026-09-24 08:15** 제출 패키지 09-24 판 작성 — `source\G80SH_삼성제출_20260924\` (00 접수용·01 기술설명서·02 첨부·03 OSD, 약 230MB).
  09-20 판 대체. OSD「외부입력 자동 전환+」는 미확인 — 제출 전 사용자 확인 필요. 야간 시험 원기록(프로그램 이름 포함)은 21항 주의에 따라 제외.
